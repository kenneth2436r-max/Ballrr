<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links: without this, the OS has no idea this app can handle
                 tournament-tracker-f343b.web.app URLs at all, so every shared follow/join link
                 always opens in the browser even with the app installed. This is the actual fix
                 for that, not just a preference the user could toggle. android:autoVerify makes
                 Android check the assetlinks.json file at
                 https://tournament-tracker-f343b.web.app/.well-known/assetlinks.json (see
                 public/.well-known/assetlinks.json) at install time to confirm this app is
                 really authorized for this domain before routing links to it silently. Without a
                 verified assetlinks.json, the OS falls back to an Open With chooser instead of
                 skipping straight to the app. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="tournament-tracker-f343b.web.app" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Needed for the in-app Voice Quick-Log feature (Web Speech API running inside the
         WebView). Without this, the browser-level speech recognition request is silently
         denied and the mic button never actually captures anything. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
</manifest>
